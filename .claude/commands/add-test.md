Look at $ARGUMENTS and add missing unit tests to achieve 100% coverage.
Follow the existing test patterns in tests/weather_pipeline/.
Mock all HTTP calls with pytest-httpx.
Use :memory: DuckDB for load tests.
